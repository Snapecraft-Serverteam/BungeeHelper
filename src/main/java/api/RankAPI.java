package api;

public class RankAPI {
    /*
    public static Boolean inGroup(ProxiedPlayer p, String group) {
        for(GroupEntityData gr : CloudAPI.getInstance().getOnlinePlayer(p.getUniqueId()).getPermissionEntity().getGroups()) {
            if(gr.getGroup().equalsIgnoreCase(group)) {
                return true;
            }
        }
        return false;
    }*/


}
